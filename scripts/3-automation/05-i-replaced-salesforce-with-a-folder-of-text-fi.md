# I replaced Salesforce with a folder of text files

**Angle:** automation · **Leverage:** high · **Proof:** A live `morning` command that reads a `contacts/` folder of plain-text files and prints a ranked digest of who to talk to — plus a kanban that renders straight from YAML. No CRM, no Airtable, no seat fees.
**Hook (0-3s):** No CRM. No Airtable. I type "morning" and a folder of text files tells me exactly who to talk to today.

## 🎬 REEL CUT (9:16, 25-40s)
**On-screen text (open):** SALESFORCE: $0/seat. (it's a folder.)
**Beat 1 — Hook (0-3s):** "No CRM. No Airtable. I type one word — `morning` — and it tells me exactly who I'm letting go cold." · *Show:* Terminal full-screen, cursor blinking, Sahil types `morning` and hits enter.
**Beat 2 — Tension (3-10s):** "Every founder I know is paying per seat for a database they feel guilty every time they open. My whole sales OS is a folder of text files." · *Show:* `ls contacts/` scrolls a directory of `.md` files — real names, one file per relationship.
**Beat 3 — Proof (10-25s):** "I talk to it in plain English. It reads every contact, ranks who's gone quiet, who I promised to follow up, who I haven't touched in 30 days — and hands me the list. The board you'd build in Airtable? It renders itself from the same text." · *Show:* The `morning` digest prints a ranked list with reasons next to each name; cut to a kanban board rendering live from a `.yaml` file.
**Beat 4 — Turn (25-32s):** "I didn't buy a CRM. I built the thing that *is* the CRM. Edit one file, the whole picture updates, nothing drifts. The AI proposes who to reach. I dispose." · *Show:* Open one contact file, add a line, re-run `morning`, the ranking shifts instantly.
**CTA (32-40s):** "If your CRM is a guilt machine you never open — DM me 'AUTOMATE.' I build these in public. Yours could be the next one I run on camera." · *On-screen card:* DM "AUTOMATE" → I'll build yours in public.
**Caption:** I deleted the CRM. My entire sales OS is a folder of text files I talk to in plain English — type `morning`, get the list. DM "AUTOMATE" to watch me build one for your business. #buildinpublic #automation #claudecode #founders #salesops #ai

## 💼 LINKEDIN CUT
**Hook line:** I cancelled the CRM. My entire sales OS is now a folder of text files I talk to in plain English.

**Body:**
Here's the whole system. One folder, `contacts/`, one plain-text file per relationship — what we last talked about, what I promised, when we last spoke. No Salesforce. No Airtable. No per-seat bill that scales with my guilt.

Every morning I type one word: `morning`. It reads the entire folder and hands me a ranked digest — who's gone quiet, who I owe a follow-up, who I haven't touched in 30 days and should. Not a dashboard I have to interpret. A list, with the reason next to each name.

The kanban view founders pay for? It renders itself from a YAML file. Same source of truth. I edit one file, the whole picture updates, and nothing drifts out of sync — because there's nothing to sync. It's all one folder.

The part I care about most: the AI proposes who to reach out to. I dispose. It can read every relationship I have, but it never sends a thing without me. Read-only by default. That's not a limitation — that's the trust model.

I'm not running a CRM. I built the thing that does the job of a CRM, in text, that I own forever, for $0 a seat.

**CTA:** If you run a business and your CRM is something you feel bad about instead of something you use, DM me "AUTOMATE." I build these in public — tell me what's eating your week and yours could be the next one I run on camera.

## 🎯 Impact unlocked from Sahil
- Screen-record the terminal running `morning` end-to-end: the command, then the ranked digest printing with a real reason beside each name (keep real names or lightly blur — the receipt is that they're real people, not dummy data).
- Capture `ls contacts/` showing the actual `.md` directory listing, and a second capture of the kanban board rendering live from the `.yaml` file — the input file and the rendered board side by side or in quick succession to sell "one source of truth, two views."
- One live edit: open a contact file, add a line, re-run `morning`, show the ranking visibly change — this is the "edit one file, everything updates, nothing drifts" proof beat.